from setuptools import setup, find_packages

setup(
    name='DownloaderV',
    version='1.0.0',
    packages=find_packages(),
    install_requires=[
        'pytube',  # Liste des dépendances requises par votre projet
    ],
    author='Zxldrok',
    author_email='assemat9@gmail.com',
    description='Téléchargeur de fichier mp4 rapides avec possibilité de choisir la qualité désiré Problème niveau api pour le mp3 donc dans la V1 il n y a que le mp4 Une interface très simple et facile à utiliser Dans les prochaines versions le logiciel se voit avec beaucoup plus d options et une interface plus travailler tout en gardant une interface simple.',
    url='https://github.com/Zxldrok/DownloaderV',
)
